# Normalize all text files to LF in the repo and on checkout, regardless of OS.
# Prevents CRLF drift on the Windows checkout (winget/kanata/nix all handle LF).
* text=auto eol=lf

# Vendored fonts are binary -- never apply line-ending conversion.
*.ttf binary
*.otf binary
